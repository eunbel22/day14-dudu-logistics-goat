# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 김은별
- **작성일**: 2026-08-06
- **대상**: 접수 원장 `shipments` 3,045건 / 25칸

---

## 오늘 내는 것 세 가지

1. **정리 방식** - 어떤 기준으로 처리했는가
2. **오류 분류** - 어떤 예외가 나왔는가
3. **처리 방안** - 다음 단계에서 어떻게 할 것인가

셋 다 **칸 하나 안에** 들어갑니다. 이 문서는 칸 하나가 한 덩어리입니다.

정리만 하고 왜 그랬는지를 안 남기면 내일 일이 진행되지 않습니다. 검산할 때 "이걸 왜 했지"가 됩니다.

정답이 없습니다. 같은 칸을 봐도 무엇을 막을지는 사람마다 다릅니다. 조사한 칸이 세 개여도 됩니다.

---

## 조사 현황

내가 본 칸에 체크합니다. **안 본 칸도 정보입니다.**

- [v] `tracking_no` - 운송장 번호
- [v] `accepted_at` - 접수 시각
- [ ] `branch_code` - 지점 코드
- [ ] `branch_name` - 지점 이름
- [ ] `sender_id` - 보내는 사람 ID
- [ ] `sender_name` - 보내는 사람 이름
- [ ] `receiver_name` - 받는 사람 이름
- [ ] `receiver_phone` - 받는 사람 전화 (이번 주 범위 밖 보류)
- [v] `receiver_area` - 받는 지역
- [ ] `receiver_dong` - 받는 동
- [v] `region_type` - 권역 구분
- [ ] `category` - 물품 분류
- [ ] `item_name` - 물품명
- [v] `weight_kg` - 무게
- [v] `width_cm` - 가로
- [v] `height_cm` - 세로
- [v] `depth_cm` - 높이
- [v] `volume_weight_kg` - 부피 무게
- [v] `billed_weight_kg` - 청구 무게
- [v] `size_grade` - 크기 등급
- [v] `price` - 요금
- [v] `eta_date` - 도착 예정일
- [ ] `status` - 배송 상태 (이번 주 범위 밖 보류)
- [v] `delivered_at` - 배송 완료 시각
- [ ] `channel` - 접수 경로 (이번 주 범위 밖 보류)

---

## 쓰기 전에 - 세 가지 눈금

칸을 채울 때 아래 셋을 골라 적습니다.

### 눈금 1. 처리 분류 - 이 예외를 무엇으로 했는가

- **수정** - 값을 고쳤다
- **삭제** - 행이나 값을 지웠다
- **통일** - 여러 표기를 하나로 묶었다
- **보류** - 손대지 않고 남겼다

### 눈금 2. 어디서 막는가 - 이 예외를 앞으로 어디서 걸러야 하는가

- **기계적 차단** - 화면이 애초에 못 넣게 한다. 필수 입력, 숫자만, 선택 목록
- **정책** - 규칙을 정해야 정해진다. 선택지를 어디까지 열지, 예외를 어디까지 허용할지
- **운영** - 사람이 받아야 한다. 상담원 연결, CS 접수, 환불 처리

세 층을 섞지 않습니다. 정책으로 정할 것을 기계로 막으면 현장이 못 씁니다. 기계로 막을 것을 사람에게 넘기면 같은 일이 계속 들어옵니다.

### 눈금 3. 확신도 - 내가 어디까지 정했는가

- **확정** - 이렇게 합니다
- **추천** - 이게 낫다고 보지만 최종 결정은 팀에서
- **선택지** - 두 갈래를 다 만들어 둡니다. 고르시면 됩니다

혼자 확정한 것보다 여럿이 같은 결론에 도달한 것이 믿을 만합니다. 혼자 판단하면 틀리기 쉽습니다.

---

# 칸별 정책

---

## 칸 - `tracking_no` (운송장 번호)

### 1. 초기 상태

- 값 종류: 3,000가지 (완전 중복 45쌍 존재)
- 빈 칸: 0건
- 형식: 숫자 (문자열 형태 운송장 번호)
- 어떻게 셌나: 파이썬/Pandas `duplicated(subset=['tracking_no'], keep=False)` 확인

### 2. 예외

**예외 1 - 완전 중복 행 (90건 / 45쌍)**

- 몇 건: 90건 (45쌍)
- 어떻게 찾았나: `tracking_no` 중복 행 검색 후 전 수치/문자열 컬럼 일치 여부 비교 (`df.duplicated()`)

### 3. 처리

**예외 1 - 완전 중복 행 (90건 / 45쌍)**

- 분류: 삭제 (한 쌍당 1건 남기고 drop_duplicates)
- 왜 이렇게 정했나: 모든 컬럼(접수시각, 송수신자, 금액, 상태 등)이 완전히 동일한 동일 접수 건이 2줄로 중복 입력된 완전 중복이므로 판단 없이 1건 제거
- 왜 생겼다고 보는가: 시스템 접수 시 네트워크/DB 트랜잭션 오류로 중복 생성 또는 중복 수집
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1 - 완전 중복 행**

- 층: 기계적 차단
- 어떻게: 저장 전 완전 중복 행 제거 및 DB/데이터베이스 테이블 수준에서 `tracking_no` UNIQUE 제약조건 추가

### 5. 못 정한 것 - 누구에게 물어야 하나

- 없음

---

## 칸 - `accepted_at` (접수 시각)

### 1. 초기 상태

- 값 종류: 3,045건
- 빈 칸: 0건
- 형식: 날짜/시간 (5가지 형태 섞임)
- 어떻게 셌나: 정규식 파싱 및 datetime 변환 테스트

### 2. 예외

**예외 1 - 날짜/시간 표기 형식 혼재 (5가지)**

- 몇 건: 3,045건 전체 중 비표준 표기 다수
- 어떻게 찾았나: 날짜 파싱 패턴 분석 (표준 대시, 2자리 연도 대시, 한국어 시분, 슬래시 YYYY/MM/DD, 슬래시 MM/DD/YYYY)

### 3. 처리

**예외 1 - 날짜/시간 표기 형식 혼재**

- 분류: 통일
- 왜 이렇게 정했나: 날짜 기반 집계 및 시간 순서 계산(eta_date, delivered_at 검증)을 위해 표기를 하나로 일치시켜야 함
- 왜 생겼다고 보는가: 서로 다른 시스템/채널(앱, 키오스크, 엑셀 수집)에서 각각 다른 포맷으로 입력됨
- 확신도: 확정 (`yyyy-mm-dd hh:mm:ss` 24시간제로 변환 완료, 파싱 실패 0건)

### 4. 앞으로 어디서 막나

**예외 1 - 날짜/시간 표기 형식 혼재**

- 층: 기계적 차단
- 어떻게: 입출력 인터페이스에서 ISO 8601 / 표준 Datetime 포맷(`yyyy-mm-dd hh:mm:ss`)으로만 입력받도록 강제

### 5. 못 정한 것 - 누구에게 물어야 하나

- 없음

---

## 칸 - `receiver_area` (받는 지역)

### 1. 초기 상태

- 값 종류: 18~25가지 (이형 표기 포함)
- 빈 칸: 0건
- 형식: 글자 (자유 입력 형태)
- 어떻게 셌나: `value_counts()` 및 유동 표기 추출

### 2. 예외

**예외 1 - 광역시도 지역명 표기 갈림 (`서울`/`서울시`/`서울특별시`/`SEOUL` 등)**

- 몇 건: 다수 건
- 어떻게 찾았나: 고유값 리스트 정렬 후 유사 명칭 확인

### 3. 처리

**예외 1 - 지역명 표기 갈림**

- 분류: 통일
- 왜 이렇게 정했나: 지역별 물량 집계 및 요금/권역(region_type) 판정이 안 되므로 표준 시/도 명칭으로 통일
- 왜 생겼다고 보는가: 입력 폼이 텍스트 자유 입력 방식이었음
- 확신도: 확정 (`서울`, `대전`, `울산` 등 대표 표준 명칭으로 통일)

### 4. 앞으로 어디서 막나

**예외 1 - 지역명 표기 갈림**

- 층: 기계적 차단
- 어떻게: 텍스트 입력창을 없애고 도로명주소 API 검색 또는 표준 시/도 드롭다운 선택으로 변경

### 5. 못 정한 것 - 누구에게 물어야 하나

- 없음

---

## 칸 - `weight_kg` (무게)

### 1. 초기 상태

- 값 종류: 숫자형 및 문자열 혼재
- 빈 칸: 0건
- 형식: 섞임 (숫자 / "2.1kg" 등 문자열)
- 어떻게 셌나: 문자열 포함 여부 검사 및 이상치(999, 음수, 0) 조건 필터링

### 2. 예외

**예외 1 - 단위 문자열 포함 ("2.1kg")**

- 몇 건: 51건
- 어떻게 찾았나: 타입 검사 및 `str.contains('kg')`

**예외 2 - 이상치 (999, 음수 -1/-3, 0)**

- 몇 건: 총 48건 (999: 11건, 음수: 20건, 0: 17건)
- 어떻게 찾았나: 수치 범위 조건문 (`weight_kg <= 0 | weight_kg >= 999`)

### 3. 처리

**예외 1 - 단위 문자열 포함 51건**

- 분류: 통일 (문자열 제거 후 float 변환)
- 왜 이렇게 정했나: 수치 계산에 사용하기 위함
- 왜 생겼다고 보는가: 단위까지 수동 입력함
- 확신도: 확정

**예외 2 - 이상치 48건**

- 분류: 수정 (37건) / 보류 (11건 결측 처리)
- 왜 이렇게 정했나: `billed_weight_kg > volume_weight_kg`인 37건은 `billed_weight_kg` 값이 곧 원래 실제 무게였으므로 복구함. 나머지 `billed_weight_kg ≈ volume_weight_kg`인 11건은 원래 무게를 추정할 수 없어 결측(NaN) 처리함
- 왜 생겼다고 보는가: 측정기 오류, 센서 에러값(999) 또는 수동 입력 오타
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1, 2 - 문자열 단위 및 이상치**

- 층: 기계적 차단
- 어떻게: 전자저울 수신값 자동 입력 또는 화면 입력 시 0 초과 30kg 이하의 양수 float만 허용하며 단위는 UI에 고정

### 5. 못 정한 것 - 누구에게 물어야 하나

- 결측 처리된 11건의 실제 무게 확인 -> 접수 지점 담당자

---

## 칸 - `width_cm`, `height_cm`, `depth_cm` (가로, 세로, 높이)

### 1. 초기 상태

- 값 종류: 숫자형 및 문자열 혼재
- 빈 칸: 0건
- 형식: 섞임 (숫자 / "32cm" 등 문자열)
- 어떻게 셌나: 조건별 필터링 및 범위 검사

### 2. 예외

**예외 1 - 단위 문자열 포함 ("32cm")**

- 몇 건: `width_cm` 46건
- 어떻게 찾았나: `str.contains('cm')`

**예외 2 - 100배 스케일 이상치 (1300~4800대)**

- 몇 건: width 5건, height 7건, depth 5건
- 어떻게 찾았나: 200cm 초과 수치 검색

**예외 3 - 에러 표시값 9999 및 0 이하 (-10, 0)**

- 몇 건: 9999 (각 3건), 0 이하 (width 9건, height 14건, depth 7건)
- 어떻게 찾았나: 0 이하 및 9999 조건 조회

### 3. 처리

**예외 1 - 단위 문자열 포함**

- 분류: 통일 ("cm" 제거 후 int 변환)
- 왜 이렇게 정했나: 부피무게 계산용
- 확신도: 확정

**예외 2 - 100배 스케일 이상치**

- 분류: 수정 (÷100 복구)
- 왜 이렇게 정했나: ÷100 시 정상 택배 박스 규격(13~48cm)에 정확히 일치하므로 단위 착오(mm 입력)로 판단하여 복원
- 확신도: 확정

**예외 3 - 9999 및 0 이하**

- 분류: 삭제 (결측 NaN 처리)
- 왜 이렇게 정했나: 물리적으로 불가능한 박스 치수이며 원래 값 복원 근거가 전혀 없음
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 - 치수 오류**

- 층: 기계적 차단
- 어떻게: 부피 측정 자동 센서 연동 또는 0 초과 200cm 이하 숫자만 입력받도록 입력 폼 검증 rule 설정

### 5. 못 정한 것 - 누구에게 물어야 하나

- 치수 결측 건의 실제 규격 -> 접수 현장

---

## 칸 - `region_type` (권역 구분)

### 1. 초기 상태

- 값 종류: 일반 / 제주 / 도서산간
- 빈 칸: 0건
- 형식: 글자
- 어떻게 셌나: 주소(receiver_area, receiver_dong)와의 십자 비교 분석

### 2. 예외

**예외 1 - 주소와 region_type 불일치 (tracking_no: 3100001129)**

- 몇 건: 1건
- 어떻게 찾았나: `receiver_area`가 "경기", `receiver_dong`이 "성남시 분당구"인데 `region_type`이 "제주"로 오염된 건 발견

### 3. 처리

**예외 1 - 불일치 1건**

- 분류: 수정 ("제주" -> "일반")
- 왜 이렇게 정했나: 육지 주소(성남시 분당구)가 명백하므로 도착 주소 기반 표준 로직으로 수정함
- 왜 생겼다고 보는가: 수동 선택 시 오클릭 오입력
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1 - 권역 오입력**

- 층: 기계적 차단
- 어떻게: `region_type`은 사용자/담당자가 직접 선택하지 못하게 하고, 입력된 도착지 주소(도서산간/제주 여부)에 따라 시스템이 자동 판정하도록 변경

### 5. 못 정한 것 - 누구에게 물어야 하나

- 없음

---

## 칸 - `delivered_at` (배송 완료 시각)

### 1. 초기 상태

- 값 종류: 날짜/시간 (결측 포함)
- 빈 칸: 322건
- 형식: 날짜/시간
- 어떻게 셌나: `status`별 결측 및 `accepted_at`과의 시각 비교 (`delivered_at < accepted_at`)

### 2. 예외

**예외 1 - 완료 상태인데 delivered_at 결측**

- 몇 건: 65건 (배송 완료류 상태이나 시각 누락)
- 어떻게 찾았나: `status` 완료 건 중 `delivered_at.isna()` 조건 필터링

**예외 2 - 논리 역전 (accepted_at보다 완료 시각이 앞섬)**

- 몇 건: 40건
- 어떻게 찾았나: `delivered_at < accepted_at` 계산. 확인 결과 `accepted_at`과 시:분:초가 100% 동일하고 날짜만 1~4일 앞당겨진 오염 패턴 확인

### 3. 처리

**예외 1 - 완료인데 결측 65건**

- 분류: 보류 (결측 유지)
- 왜 이렇게 정했나: 실제 배송완료 시각을 임의로 추정하여 채울 근거가 없음
- 확신도: 추천 (추후 기사 앱 스캔 로그 조회가 필요함)

**예외 2 - 논리 역전 40건**

- 분류: 삭제 (결측 NaN 처리)
- 왜 이렇게 정했나: `accepted_at` 시분초 복사 후 날짜 오염된 데이터로 신뢰할 수 없는 오염값임
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1, 2 - 배송완료 시각 오류**

- 층: 기계적 차단 및 운영
- 어떻게: 배송기사 앱에서 배송완료 스캔 시 서버 시각이 자동 입력되게 하고, `delivered_at >= accepted_at` validation 검사 적용

### 5. 못 정한 것 - 누구에게 물어야 하나

- 완료 상태이나 `delivered_at`이 누락된 65건의 실제 완료 시각 -> 배송 담당 기사/시스템 로그 확인

---

## 칸 - `size_grade` (크기 등급)

### 1. 초기 상태

- 값 종류: 극소형, 소형, 중형, 대형 및 이형 표기(극소, 소, 중, 대)
- 빈 칸: 65건
- 형식: 글자
- 어떻게 셌나: 사내 원장 v1 접수 규정 공식 대입 검증

### 2. 예외

**예외 1 - 이형 표기 (극소/소/중/대)**

- 몇 건: 다수 건
- 어떻게 찾았나: 유동 표기 리스트

**예외 2 - 결측 65건**

- 몇 건: 65건
- 어떻게 찾았나: `isna()` 조회

### 3. 처리

**예외 1 - 이형 표기**

- 분류: 통일 (극소형/소형/중형/대형)
- 확신도: 확정

**예외 2 - 결측 65건**

- 분류: 수정 (규정 공식 계산 적용)
- 왜 이렇게 정했나: 사내 원장 v1 접수 규정(세 변의 합 등급 vs billed_weight 등급 중 큰 쪽) 공식을 기존 정상 데이터 2,879건에 대입 시 99.3% 일치함을 확인했으므로, 검증된 공식으로 결측 65건을 정확히 채움
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 - 등급 오입력 및 누락**

- 층: 기계적 차단
- 어떻게: 수동 선택을 막고, 입력된 가로/세로/높이와 요금무게를 기반으로 시스템이 자동 산출하도록 구현

### 5. 못 정한 것 - 누구에게 물어야 하나

- 기존 값이 있으나 공식과 불일치하는 20건의 재산정 여부 -> 운영/영업팀 정책 판단

---

## 칸 - `price` (요금)

### 1. 초기 상태

- 값 종류: 숫자 (결측 215건, 음수 4건 포함)
- 빈 칸: 215건
- 형식: 숫자
- 어떻게 셌나: 접수 규정 요금표 대입 검증

### 2. 예외

**예외 1 - 결측 215건 및 음수/0 이하 4건**

- 몇 건: 총 219건 (결측 215건, 음수/0 이하 4건)
- 어떻게 찾았나: `isna() | price <= 0` 필터링

### 3. 처리

**예외 1 - 결측 215건 및 0 이하 4건**

- 분류: 수정 (규정 요금표 대입 계산)
- 왜 이렇게 정했나: 규정 요금표(`size_grade` x `region_type` 매트릭스)를 기존 정상 데이터 2,681건에 대입 검증한 결과 96.5% 일치함을 확인하였으므로, 결측 및 음수/0 이하 오류 건은 규정 요금표에 따라 정상 금액으로 자동 계산하여 채움
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 - 요금 오입력/누락**

- 층: 기계적 차단
- 어떻게: 요금 수동 입력 필드를 폐지하고 등급과 권역 산출 결과에 따라 요금이 자동 계산되어 수금되도록 시스템 구성

### 5. 못 정한 것 - 누구에게 물어야 하나

- 요금표와 일치하지 않는 기존 93건의 승인 요금 할인 여부 -> 정산팀

---

# 마무리

## 검산

정한 대로 실제로 됐는지 대 봅니다. 맞히려는 것이 아니라 **어디가 다른지 보려는 것**입니다.

**주요 칸 검산 결과**

- `tracking_no`: 완전 중복 90건(45쌍) 삭제 완료 후 3,000건 유지 예정
- `weight_kg`: 문자열 단위 51건 통일, 이상치 37건 복구, 11건 결측 처리
- `width_cm / height_cm / depth_cm`: 단위 통일 완료, 100배 튄 17건 정상 복구, 9999 및 0 이하 결측 처리
- `size_grade` & `price`: 접수 규정 공식 대입 검증(각각 99.3%, 96.5% 일치율 확증)을 통해 결측 65건/215건에 대해 신뢰성 높은 자동 계산 적용 완료

## 원본 보존

- [v] 원래 칸은 그대로 두고 새 칸을 옆에 만들었다 (`_raw` 컬럼 보존 방식)
- [ ] 원래 칸을 덮어썼다

새로 만든 칸:

- `weight_kg_raw`
- `width_cm_raw`
- `billed_weight_kg_raw`
- `receiver_area_raw`
- `size_grade_raw`
- `accepted_at_raw`
- `eta_date_raw`

원래 칸을 덮어썼다면 되돌릴 방법:

- `_raw` 접미사가 붙은 원본 데이터 컬럼을 보존하여 언제든 원본 값 조회 및 복원 가능

## 이번에 다루지 않기로 한 것

안 하기로 정하는 것도 산출물입니다. 하나여도 됩니다.

- `status` (배송 상태), `channel` (접수 경로), `receiver_phone` (전화번호) 이형 표기 정리는 이번 주 작업 범위 밖으로 보류함
- 접수 금지 품목 검사 로직 및 도착 예정일(`eta_date`) 산출 로직 검증은 추후 과제로 이관함

## 다음 사람에게

내일 이 폴더를 여는 사람이 나여도 오늘 무엇을 왜 했는지 기억나지 않습니다. 같은 조사를 두 번 하지 않으려면 무엇을 알아야 합니까.

- 데이터 정제 시 값의 무조건적인 삭제/변경을 지양하고, `billed_weight_kg` 등 상호 관련 컬럼 간의 복구 가능성을 우선 검증하였음
- 접수 규정 문서의 규칙과 실제 원장 간 일치율을 교차 검증하여 방침을 세웠으므로 참고할 것

## 막힌 자리

- 없음

---

## 내일 미리 보기

내일은 접수 키오스크 입력 폼을 만듭니다. 택배 정책은 CJ대한통운, 화면은 CU 편의점 키오스크를 참고합니다.

각 칸의 **4번(어디서 막나)** 이 그대로 내일의 설계도입니다.

- **기계적 차단**으로 표시한 것 -> 내일 화면에서 구현한다
- **정책**으로 표시한 것 -> 선택지를 열어 두고 협의 대상으로 남긴다
- **운영**으로 표시한 것 -> 상담원 연결이나 문의 버튼으로 뺀다

키오스크를 안 써봤으면 유튜브 브이로그를 5개 봅니다. 화면이 바뀌는 단위로 따라가며 보면 대부분 해결됩니다. 5개 중 3개 이상에서 같은 것이 반복되면 그건 그렇다고 봐도 됩니다.

