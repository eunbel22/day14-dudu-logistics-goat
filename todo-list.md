# 두두택배 shipments 원장 정리 기록

> 원본: `0806-dudu-logistics-shipments_-_김은별__1_.csv` (3,045행 x 25칸)
> 참고 문서: 00_표준절차_6단계.md, 03_데이터_사전.md, 04_택배업_기본지식.md, 두두택배 접수 규정(사내 원장 v1)

---

## 1단계 - 전체 보기 (완료)

- 3,045행 x 25칸, 데이터 사전과 칸 구성 일치
- branch_code/name 6종류(정상), category 8종류(정상)
- 처음엔 receiver_area 18~25종류, size_grade 5~11종류, status 16종류, channel 11종류로 이형 표기 다수 확인

## 2단계 - 칸별 조사 (완료)

주요 문제만 요약 (상세는 아래 항목별 기록 참고)

| 칸 | 문제 |
|---|---|
| tracking_no | 완전 중복 90건(45쌍) |
| weight_kg | 타입 혼재(문자열 "2.1kg" 등), 0 이하/음수/999 이상치 |
| width_cm | 타입 혼재(문자열 "32cm" 등), 0 이하·200 초과 이상치 |
| height_cm / depth_cm | 0 이하·200 초과 이상치 |
| billed_weight_kg | 타입 혼재(문자열) |
| receiver_area | 서울/서울시/서울특별시/SEOUL 등 이형 표기 |
| size_grade | 극소형/극소, 중형/중 등 이형 표기 + 결측 |
| accepted_at | 대시·슬래시(연도 앞/뒤)·2자리 연도·한국어 시분 등 5가지 형식 혼재 |
| eta_date | 대시·점·슬래시 3가지 형식 혼재 |
| region_type | receiver_area/receiver_dong과 불일치 1건 |
| volume_weight_kg | 결측 87건 |
| price | 결측 215건, 0 이하 4건 |
| delivered_at | 결측 322건(정상 257 + 진짜결측 65), 논리 역전 40건 |
| status / channel / receiver_phone | 이형 표기 다수 — 이번 주 범위 밖으로 보류 |

---

## 형식 통일 — 실행 완료 (csv에 이미 적용됨)

값의 옳고 그름 판단이 아니라 "같은 걸 다르게 적은 걸 하나로 맞추는" 작업. 아래 7개는 이미 `shipments_원본.csv`에 실행 완료.

1. **weight_kg**: "2.1kg" 같은 문자열 51건에서 "kg" 제거 후 숫자형(float) 변환. 원본은 `weight_kg_raw`에 보존
2. **width_cm**: "32cm" 같은 문자열 46건에서 "cm" 제거 후 숫자형(int) 변환. 원본은 `width_cm_raw`에 보존
3. **billed_weight_kg**: 전체 문자열 타입 → 숫자형(float) 변환. 원본은 `billed_weight_kg_raw`에 보존
4. **receiver_area**: 서울/서울시/서울특별시/SEOUL → 서울, 대전/대전시/대전광역시 → 대전, 울산/울산시/울산광역시 → 울산으로 통일. 원본은 `receiver_area_raw`에 보존
5. **size_grade(표기만)**: 중→중형, 극소→극소형, 대→대형, 소→소형으로 통일. 원본은 `size_grade_raw`에 보존 (결측·초과·규격외 등 값 자체의 처리는 3단계에서 별도 진행)
6. **accepted_at**: 5가지 형식(표준 대시, 2자리 연도 대시, 한국어 시분, 슬래시 YYYY/MM/DD, 슬래시 MM/DD/YYYY)을 전부 파싱해서 `yyyy-mm-dd hh:mm:ss`(24시간제)로 통일. 파싱 실패 0건. 원본은 `accepted_at_raw`에 보존
7. **eta_date**: 대시·점·슬래시 3가지 형식을 `yyyy-mm-dd`로 통일. 원본은 `eta_date_raw`에 보존

**volume_weight_kg 결측 87건**도 위 형식 통일 이후 공식(가로x세로x높이/6000)으로 계산해서 채움 — 단, 이때 쓴 치수 중 일부가 아직 정리 안 된 이상치였을 수 있어 아래 3단계에서 재계산 예정.

---

## 3단계 - 처리 방침

> 2단계 조사에서 찾은 문제마다 "앞으로 어떻게 할지"를 정한 기록. csv에는 아직 적용 안 함 — 방침만 확정.

## width_cm / height_cm / depth_cm 이상치 (확정)

**조사에서 본 것**

- `9999` 값: width 3건, height 3건, depth 3건 → 정상 범위와 동떨어짐, 결측/에러 표시로 판단
- `0` 또는 `-10` 값: width 9건, height 14건, depth 7건 → 물리적으로 불가능한 값
- 100배 튄 값(예: 4200, 3700, 1300 등): width 5건, height 7건, depth 5건 → 100으로 나누면 정상 범위(13~48cm)로 들어감. 단위 착오(mm 입력 오류 등)로 추정

**정한 방침**

| 패턴 | 값 | 방침 | 근거 |
|---|---|---|---|
| 100배 튄 값 | 1300~4800대 | **÷100 해서 복구** | 나누면 정상 범위로 정확히 들어맞음 — 원래값 복원 가능하다고 판단 |
| 9999 | 9999 | **결측 처리(NaN)** | 정상값 복원 불가, 명백한 에러 표시값 |
| 0 이하 | 0, -10 | **결측 처리(NaN)** | 물리적으로 불가능, 복원 근거 없음 |

**그래서 만들 기능**

- 치수 입력값 검증 함수: 0 이하 또는 9999는 결측 처리, 200 초과 값 중 ÷100 해서 정상 범위(0 초과 200 이하)로 들어가면 복구, 아니면 결측 처리

**아직 다루지 않은 것**

- 복구/결측 처리 후 volume_weight_kg 재계산 (치수 이상치 해결 이후 순서로 진행 예정)

---

## tracking_no 중복 (확정)

**조사에서 본 것**

- 90건(45쌍)의 tracking_no 중복 발견
- 확인 결과, 90건 전부가 **완전 중복 행** — 같은 접수 1건이 원장에 두 줄로 그대로 들어간 것 (accepted_at, sender_name, receiver_name, status, price 등 모든 칸이 두 행에서 완전히 동일)
- 값이 서로 다른 진짜 충돌 케이스는 없었음

**정한 방침**

- 한 쌍당 한 줄만 남기고 나머지는 삭제(drop_duplicates)
- 이유: 어느 값이 맞는지 판단할 필요가 없는 완전 중복이므로 그대로 제거하면 됨

**그래서 만들 기능**

- 저장 전 완전 중복 행 제거
- 저장 시 tracking_no unique 제약 추가 (앞으로 같은 번호가 두 번 들어오는 것 자체를 막음)

---

## weight_kg 이상치 (확정)

**조사에서 본 것**

- 이상치 총 48건: 999(11건), 음수 -1/-3(20건), 0(17건)
- width/height/depth_cm은 대부분 정상이라 volume_weight_kg는 정상 계산 가능
- billed_weight_kg(= max(weight_kg, volume_weight_kg))는 오염되지 않고 원래 값을 대부분 유지하고 있음을 확인

**복구 가능성 검증**

- billed_weight_kg > volume_weight_kg 인 경우 → weight_kg가 원래 max였다는 뜻 → billed_weight_kg 값이 곧 원래 weight_kg → **37건 복구 가능**
- billed_weight_kg ≈ volume_weight_kg 인 경우 → 원래도 volume이 max였음 → weight_kg 원래값은 알 수 없음 → **11건 복구 불가**

**정한 방침**

| 조건 | 건수 | 방침 |
|---|---|---|
| billed_weight_kg > volume_weight_kg | 37건 | weight_kg = billed_weight_kg 값으로 복구 |
| billed_weight_kg ≈ volume_weight_kg | 11건 | weight_kg 결측 처리(NaN) — 복구 근거 없음 |

**그래서 만들 기능**

- weight_kg 이상치(999/음수/0) 발견 시: volume_weight_kg 재계산 → billed_weight_kg와 비교 → billed > volume이면 billed_weight_kg 값으로 weight_kg 복구, 아니면 결측 처리
- 이후 billed_weight_kg는 복구된(또는 결측 처리된) weight_kg와 volume_weight_kg로 다시 계산

**아직 다루지 않은 것**

- weight_kg가 결측으로 남은 11건의 billed_weight_kg 최종 재계산 (volume_weight_kg 이상치 정리 이후 순서로 진행 예정)

---

## region_type ↔ receiver_area 불일치 (확정)

**조사에서 본 것**

- 불일치 1건: tracking_no 3100001129 — receiver_area="경기", receiver_dong="성남시 분당구"(명백한 육지 주소)인데 region_type만 "제주"로 오염됨
- 반대 방향(주소는 제주인데 region_type이 다른 경우) 0건
- 도서산간(울릉/백령/흑산/거문/추자) 지명과 region_type 불일치도 0건 — 이 부분은 전부 정상

**정한 방침**

- region_type은 receiver_area/receiver_dong(도착 주소)에 종속되는 값이므로, **주소 기준으로 region_type을 재계산**
  - receiver_area가 "제주" → 일반
  - receiver_dong에 울릉/백령/흑산/거문/추자 포함 → 도서산간
  - 그 외 → 일반
- 이번 1건: receiver_area="경기" → region_type을 "일반"으로 수정

**그래서 만들 기능**

- region_type을 원장 값 그대로 쓰지 않고, receiver_area/receiver_dong으로 매번 재계산하는 함수로 대체

---

## delivered_at (확정)

**조사에서 본 것**

- 전체 결측 322건
  - 완료 아닌 상태(미배송/반품/배송중 등)라서 결측인 경우 257건 → 아직 배송 안 끝난 게 맞으므로 정상
  - 완료류 상태인데 결측인 경우 65건 → 진짜 결측
- delivered_at < accepted_at (논리 역전) 40건
  - 전부 accepted_at과 시:분:초가 정확히 동일하고 날짜만 1~4일 앞당겨져 있음
  - 정상 배송 건은 시:분:초가 accepted_at과 같은 비율이 0.1%뿐인데 역전 건은 100% 동일 → accepted_at을 복사해 날짜만 오염시킨 값으로 판단, 실제 배송완료 시각이 아님

**정한 방침**

| 유형 | 건수 | 방침 |
|---|---|---|
| 완료 아닌 상태의 결측 | 257건 | 그대로 둠 (정상) |
| 완료인데 결측 | 65건 | 그대로 결측 유지 (복구 근거 없음) |
| 논리 역전 | 40건 | 결측 처리(NaN) — accepted_at 복사로 만들어진 오염값으로 판단, 신뢰 불가 |

**그래서 만들 기능**

- delivered_at < accepted_at인 값은 저장/조회 시 자동으로 결측 처리
- status가 완료류인데 delivered_at이 없는 65건은 별도 목록으로 남겨 추후 확인 대상으로 표시(이번엔 값 채우지 않음)

---

## size_grade 결측 (확정 — 접수규정 문서 확보로 방침 전환)

**조사에서 본 것**

- 결측 65건. 등급 구간 기준이 없어 보류 중이었으나, 두두택배 접수 규정(사내 원장 v1) 확보로 해결
- 규정 공식: 세 변의 합(가로+세로+높이)과 요금 무게(billed_weight_kg) 각각으로 등급을 매겨 **더 높은 등급**을 적용
  - 극소형: 세 변의 합 60cm 이하 & 2kg 이하 / 소형: 80cm 이하 & 5kg 이하 / 중형: 120cm 이하 & 15kg 이하 / 대형: 160cm 이하 & 25kg 이하 / 둘 다 초과하면 접수 불가
- 이 공식을 기존 값 있는 2,879건(이상치 제외 깨끗한 표본)에 대입해 검증 → **99.3%(2,859건) 일치**. 나머지는 공식 문제가 아니라 size_grade 자체의 무작위 오염값으로 판단

**정한 방침**

- 결측 65건은 규정 공식으로 계산해서 채움
- 기존에 값이 있지만 공식과 다른 20건(불일치)은 이번엔 안 건드림 — "결측만 채운다"는 원칙 유지, 있는 값을 덮어쓰는 건 별도 논의 필요

**그래서 만들 기능**

- size_grade 계산 함수: 세 변의 합 등급 vs 요금무게 등급 중 큰 쪽 채택

---

## price 결측/이상치 (확정 — 접수규정 문서 확보로 방침 전환)

**조사에서 본 것**

- 결측 215건, 0 이하 4건. 요금표가 없어 보류 중이었으나, 접수 규정 확보로 해결
- 규정 요금표: size_grade x region_type 조합으로 금액 결정 (예: 중형+일반=6,000원)
- 기존 값 있는 2,681건(깨끗한 표본)에 대입 검증 → **96.5%(2,588건) 일치**. 나머지는 요금표 자체의 무작위 오염값으로 판단

**정한 방침**

- 결측 215건 + 0 이하 4건은 규정 요금표로 계산해서 채움
- 기존에 값이 있지만 요금표와 다른 건들은 이번엔 안 건드림 (size_grade와 동일한 원칙)

**그래서 만들 기능**

- price 계산 함수: (size_grade, region_type) → 요금표 조회

**아직 다루지 않은 것**

- 접수 규정 5절의 "접수 금지 품목" 검사 로직 (이번 3단계 범위 밖, 필요 시 별도 항목으로 추가)
- 규정 6절의 도착 예정일(eta_date) 계산 로직 검증 (이번엔 형식 통일만 진행, 값 자체 검증은 안 함)